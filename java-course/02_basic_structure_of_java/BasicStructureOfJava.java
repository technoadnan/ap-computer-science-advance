public class BasicStructureOfJava {
   // function --> only certain work
   // class --> combination of diff function
   // object --> instance of class
   public static void main(String[] args) {
      // public --> access modifier; Since the main method needs to be called by the (JRE), it must be public
      // static --> main method belongs to the class itself rather than an instance of the class
      // void --> this function doesn't return anything
      // main(String[] args) --> entry point; array of String objects that stores CLI
   }
}
